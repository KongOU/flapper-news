source 'https://rubygems.org'
gem 'rails', '4.2.3'
gem 'sqlite3'
gem 'sass-rails', '~> 5.0'
gem 'uglifier', '>= 1.3.0'
gem 'jbuilder', '~> 2.0'
gem 'sdoc', '~> 0.4.0', group: :doc
# gem 'bcrypt', '~> 3.1.7'
# gem 'unicorn'
# gem 'capistrano-rails', group: :development

# Devise
gem 'devise'

# Angular Rails Template
gem 'angular-rails-templates'

# Rails automatically send the cookie to Angular and also validate the header when Angular is submitting data
gem 'angular_rails_csrf'

# The controller-level `respond_to' feature has been extracted to the `responders` gem
gem 'responders'

group :development, :test do
  gem 'byebug'
  gem 'web-console', '~> 2.0'
  gem 'spring'
  gem 'better_errors'
end

